import React, {Component} from 'react';

import Prison from "./prison.js";

const PrisonList = props => {
  return (
    
    <ul>
      {/*{props.data.map(data => {
        return <Prison key={data.prison.id} prison={Prison} />;
      })}*/}<h1>test</h1>
      
    </ul>
   ,
    console.log(props)
  );
};

export default PrisonList;
